'use client'
import { ReactNode } from 'react'
// Placeholder — replaced by Wave 1 Agent A
export function Topbar({ actions }: { actions?: ReactNode }) {
  return <div className="topbar" data-testid="topbar">{actions}</div>
}
